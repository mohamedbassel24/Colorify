{
    "name": "api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "node ./bin/www",
        "setup": "npm install && cd colorify && npm install && npm run build",
        "heroku-postbuild": "cd colorify && npm install && npm run build"
    },
    "dependencies": {
        "concurrently": "^5.2.0",
        "cookie-parser": "~1.4.4",
        "cors": "^2.8.5",
        "debug": "~2.6.9",
        "express": "~4.16.1",
        "http-errors": "~1.6.3",
        "jade": "~1.11.0",
        "morgan": "~1.9.1",
        "multer": "^1.4.2"
    }
}